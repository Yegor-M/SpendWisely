Review the staged changes or last commit. Check for:

- bugs, edge cases, unhandled errors
- unintended side effects or regressions
- hardcoded values, debug code, console.logs left in
- naming consistency with existing codebase
- anything that looks incomplete or out of place

Be concise. Flag real issues only, skip nitpicks.